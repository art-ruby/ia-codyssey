# A1-1 Python 프롬프트 관리 프로그램 결과 보고서

**과제명**: Python & Git 기초 — Git과 함께하는 Python 첫 발자국  
**작성자**: art-ruby  
**작성일**: 2026년 8월 17일  
**제출물 위치**: https://github.com/art-ruby/ia-codyssey/tree/main/assignments/A1-1/prompt-manager

---

## 목차

1. [과제 개요](#1-과제-개요)
2. [개발 환경](#2-개발-환경)
3. [실제 작업 진행 순서](#3-실제-작업-진행-순서)
4. [프로그램 구조 및 코드](#4-프로그램-구조-및-코드)
5. [기능 시연](#5-기능-시연)
6. [Git 커밋 기록](#6-git-커밋-기록)
7. [요구사항 충족 확인](#7-요구사항-충족-확인)
8. [학습 성과](#8-학습-성과)

---

## 1. 과제 개요

### 과제 목표
GenAI 미션에서 작성한 프롬프트들이 흩어져 있는 문제를 해결하기 위해, **Python 콘솔 애플리케이션**으로 프롬프트를 관리하고, **Git/GitHub**로 버전을 관리하는 프로젝트 수행.

### 요구 사항
- ✅ 메뉴 기반 콘솔 프로그램 (번호 입력 → 기능 실행)
- ✅ 7개 기능 이상 구현
- ✅ 프롬프트 3개 이상 기본 등록
- ✅ 리스트·딕셔너리 데이터 구조 사용
- ✅ 함수 단위 코드 분리
- ✅ Git 커밋 10개 이상
- ✅ 브랜치 생성 및 병합 1회 이상
- ✅ Git 명령어 사용 (init, add, commit, push, pull, checkout, merge)

---

## 2. 개발 환경

```
OS: Windows 11 Pro (10.0.26100)
Python: 3.11.9
Git: 2.55.0.windows.1
IDE: Visual Studio Code
Terminal: PowerShell (VSCode 통합 터미널)
Git User: art-ruby (beautimind01@gmail.com)
Repository: https://github.com/art-ruby/ia-codyssey
```

---

## 3. 실제 작업 진행 순서

### 3-1단계: 프로젝트 폴더 열기

**작업 위치 확인**
```powershell
cd C:\ia-codyssey\assignments\A1-1\prompt-manager
Get-Location
```

**결과**:
```
Path
----
C:\ia-codyssey\assignments\A1-1\prompt-manager
```

**VSCode에서 폴더 열기**: `File > Open Folder > prompt-manager`

---

### 3-2단계: Git 저장소 초기화

```powershell
git init
git config user.name "art-ruby"
git config user.email "beautimind01@gmail.com"
git config --global init.defaultBranch main
```

**결과**:
```
Initialized empty Git repository in C:\ia-codyssey\assignments\A1-1\prompt-manager\.git
```

**확인**:
```powershell
git status
```

**결과**:
```
On branch main

No commits yet

nothing to commit
```

---

### 3-3단계: .gitignore 파일 생성

**VSCode 터미널에서 파일 생성**:
```powershell
@"
__pycache__/
*.pyc
*.pyo
.vscode/
.env
"@ | Out-File -Encoding utf8 .gitignore
```

**파일 확인**:
```powershell
Get-Content .gitignore
```

**결과**:
```
__pycache__/
*.pyc
*.pyo
.vscode/
.env
```

**첫 번째 커밋**:
```powershell
git add .gitignore
git commit -m "chore: .gitignore 파일 생성"
```

**결과**:
```
[main (root-commit) 8c43d96] chore: .gitignore 파일 생성
 1 file changed, 5 insertions(+)
 create mode 100644 .gitignore
```

---

### 3-4단계: data.py 작성 (기본 프롬프트 데이터)

**VSCode 파일 생성**: `New File > data.py`

**파일 내용 입력**:
```python
# 카테고리 목록
CATEGORIES = ["텍스트 생성", "이미지 생성", "영상 생성", "페르소나", "자동화", "기타"]

# 기본 프롬프트 데이터
prompts = [
    {
        "title": "블로그 글 작성 도우미",
        "content": "당신은 10년 경력의 전문 블로거입니다. 주어진 주제에 대해 SEO에 최적화된 블로그 글을 작성해주세요. 서론, 본론, 결론 구조를 갖추고, 독자의 관심을 끄는 제목을 3개 제안해주세요.",
        "category": "텍스트 생성",
        "favorite": False
    },
    {
        "title": "제품 썸네일 생성",
        "content": "다음 제품의 매력적인 썸네일 이미지를 생성해주세요. 배경색은 미니멀하게, 제품은 중앙에 배치하세요.",
        "category": "이미지 생성",
        "favorite": False
    },
    {
        "title": "IT 컨설턴트 페르소나",
        "content": "당신은 15년 경력의 IT 솔루션 컨설턴트입니다. 기업의 디지털 전환 전략을 수립하고 기술 도입을 자문합니다.",
        "category": "페르소나",
        "favorite": False
    }
]
```

**파일 저장**: `Ctrl+S`

**커밋**:
```powershell
git add data.py
git commit -m "feat: 기본 프롬프트 데이터 추가"
```

**결과**:
```
[main 2198c45] feat: 기본 프롬프트 데이터 추가
 1 file changed, 23 insertions(+)
 create mode 100644 data.py
```

---

### 3-5단계: prompt_manager.py 기본 구조 작성

**VSCode 파일 생성**: `New File > prompt_manager.py`

**기본 메뉴 함수**:
```python
from data import CATEGORIES, prompts

def show_menu():
    """메뉴를 출력하는 함수"""
    print("\n" + "="*40)
    print("    나만의 프롬프트 관리")
    print("="*40)
    print("1. 프롬프트 추가")
    print("2. 프롬프트 목록")
    print("3. 카테고리별 조회")
    print("4. 프롬프트 검색")
    print("5. 프롬프트 상세 보기")
    print("6. 즐겨찾기 관리")
    print("7. 즐겨찾기 목록")
    print("0. 종료")
    print("="*40)

def main():
    """메인 루프"""
    while True:
        show_menu()
        choice = input("선택: ").strip()
        
        if choice == "0":
            print("프로그램을 종료합니다.")
            break
        else:
            print("잘못된 선택입니다. 다시 입력해주세요.")

if __name__ == "__main__":
    main()
```

**파일 저장**: `Ctrl+S`

**테스트 실행**:
```powershell
python prompt_manager.py
```

(메뉴 화면 확인 후 `0` 입력으로 종료)

**커밋**:
```powershell
git add prompt_manager.py
git commit -m "feat: 프로그램 기본 구조 및 메뉴 구현"
```

**결과**:
```
[main c3de5b9] feat: 프로그램 기본 구조 및 메뉴 구현
 1 file changed, 30 insertions(+)
 create mode 100644 prompt_manager.py
```

---

### 3-6단계: 프롬프트 추가 기능 구현

**prompt_manager.py에 add_prompt() 함수 추가**:
```python
def add_prompt():
    """새로운 프롬프트를 추가하는 함수"""
    print("\n" + "="*40)
    print("프롬프트 추가")
    print("="*40)
    
    # 제목 입력
    while True:
        title = input("제목: ").strip()
        if title:
            break
        print("제목을 입력해주세요.")
    
    # 내용 입력
    while True:
        content = input("내용: ").strip()
        if content:
            break
        print("내용을 입력해주세요.")
    
    # 카테고리 선택
    print("\n카테고리 선택:")
    for i, category in enumerate(CATEGORIES, 1):
        print(f"{i}) {category}")
    
    while True:
        try:
            choice = int(input("선택: "))
            if 1 <= choice <= len(CATEGORIES):
                category = CATEGORIES[choice - 1]
                break
            else:
                print("올바른 번호를 입력해주세요.")
        except ValueError:
            print("숫자를 입력해주세요.")
    
    # 프롬프트 추가
    new_prompt = {
        "title": title,
        "content": content,
        "category": category,
        "favorite": False
    }
    prompts.append(new_prompt)
    print(f"\n✅ '{title}' 프롬프트가 추가되었습니다!")
```

**main() 함수 수정**:
```python
def main():
    while True:
        show_menu()
        choice = input("선택: ").strip()
        
        if choice == "0":
            print("프로그램을 종료합니다.")
            break
        elif choice == "1":
            add_prompt()
        else:
            print("잘못된 선택입니다. 다시 입력해주세요.")
```

**파일 저장 후 테스트**:
```powershell
python prompt_manager.py
```

**커밋**:
```powershell
git add prompt_manager.py
git commit -m "feat: 프롬프트 추가 기능 구현"
```

---

### 3-7단계: 프롬프트 목록 보기 기능

**prompt_manager.py에 show_list() 함수 추가**:
```python
def show_list():
    """저장된 모든 프롬프트를 보는 함수"""
    print("\n" + "="*40)
    print("프롬프트 목록")
    print("="*40)
    
    if not prompts:
        print("저장된 프롬프트가 없습니다.")
        return
    
    for i, prompt in enumerate(prompts, 1):
        favorite_mark = "⭐" if prompt["favorite"] else " "
        print(f"{i}. [{prompt['category']}] {prompt['title']} {favorite_mark}")
    
    print(f"\n총 {len(prompts)}개의 프롬프트")
```

**main()에 추가**:
```python
elif choice == "2":
    show_list()
```

**테스트 실행**:
```powershell
python prompt_manager.py
# 선택: 2
```

**커밋**:
```powershell
git add prompt_manager.py
git commit -m "feat: 프롬프트 목록 보기 기능 구현"
```

---

### 3-8단계: 프롬프트 상세 보기 기능

**prompt_manager.py에 show_detail() 함수 추가**:
```python
def show_detail():
    """선택한 프롬프트의 상세 정보를 보는 함수"""
    print("\n" + "="*40)
    print("프롬프트 상세 보기")
    print("="*40)
    
    if not prompts:
        print("저장된 프롬프트가 없습니다.")
        return
    
    try:
        num = int(input("번호 입력: "))
        if 1 <= num <= len(prompts):
            prompt = prompts[num - 1]
            favorite_mark = "⭐" if prompt["favorite"] else " "
            
            print("\n" + "-"*40)
            print(f"제목: {prompt['title']}")
            print(f"카테고리: {prompt['category']}")
            print(f"즐겨찾기: {favorite_mark}")
            print("-"*40)
            print(f"내용:\n{prompt['content']}")
            print("-"*40)
        else:
            print("존재하지 않는 번호입니다.")
    except ValueError:
        print("숫자를 입력해주세요.")
```

**커밋**:
```powershell
git add prompt_manager.py
git commit -m "feat: 프롬프트 상세 보기 기능 구현"
```

---

### 3-9단계: 프롬프트 검색 기능

**prompt_manager.py에 search_prompt() 함수 추가**:
```python
def search_prompt():
    """키워드로 프롬프트를 검색하는 함수"""
    print("\n" + "="*40)
    print("프롬프트 검색")
    print("="*40)
    
    if not prompts:
        print("저장된 프롬프트가 없습니다.")
        return
    
    keyword = input("검색어: ").strip()
    
    if not keyword:
        print("검색어를 입력해주세요.")
        return
    
    results = []
    for i, prompt in enumerate(prompts):
        if keyword.lower() in prompt["title"].lower() or keyword.lower() in prompt["content"].lower():
            results.append((i + 1, prompt))
    
    if results:
        print(f"\n검색 결과:")
        for num, prompt in results:
            favorite_mark = "⭐" if prompt["favorite"] else " "
            print(f"{num}. [{prompt['category']}] {prompt['title']} {favorite_mark}")
        print(f"\n{len(results)}개의 프롬프트를 찾았습니다.")
    else:
        print(f"\n'{keyword}'를 포함하는 프롬프트가 없습니다.")
```

**커밋**:
```powershell
git add prompt_manager.py
git commit -m "feat: 프롬프트 검색 기능 구현"
```

---

### 3-10단계: 카테고리별 조회 기능

**prompt_manager.py에 show_by_category() 함수 추가**:
```python
def show_by_category():
    """카테고리별로 프롬프트를 조회하는 함수"""
    print("\n" + "="*40)
    print("카테고리별 조회")
    print("="*40)
    
    if not prompts:
        print("저장된 프롬프트가 없습니다.")
        return
    
    print("\n카테고리 선택:")
    for i, category in enumerate(CATEGORIES, 1):
        print(f"{i}) {category}")
    
    try:
        choice = int(input("\n선택: "))
        if 1 <= choice <= len(CATEGORIES):
            selected_category = CATEGORIES[choice - 1]
            
            print(f"\n[{selected_category}] 카테고리 프롬프트:")
            results = []
            for i, prompt in enumerate(prompts):
                if prompt["category"] == selected_category:
                    results.append((i + 1, prompt))
            
            if results:
                for num, prompt in results:
                    favorite_mark = "⭐" if prompt["favorite"] else " "
                    print(f"{num}. {prompt['title']} {favorite_mark}")
                print(f"\n총 {len(results)}개의 프롬프트")
            else:
                print(f"{selected_category} 카테고리에 프롬프트가 없습니다.")
        else:
            print("올바른 번호를 입력해주세요.")
    except ValueError:
        print("숫자를 입력해주세요.")
```

**커밋**:
```powershell
git add prompt_manager.py
git commit -m "feat: 카테고리별 조회 기능 구현"
```

---

### 3-11단계: 즐겨찾기 추가/제거 기능

**prompt_manager.py에 toggle_favorite() 함수 추가**:
```python
def toggle_favorite():
    """프롬프트의 즐겨찾기를 추가/해제하는 함수"""
    print("\n" + "="*40)
    print("즐겨찾기 관리")
    print("="*40)
    
    if not prompts:
        print("저장된 프롬프트가 없습니다.")
        return
    
    try:
        num = int(input("프롬프트 번호 입력: "))
        if 1 <= num <= len(prompts):
            prompt = prompts[num - 1]
            
            if prompt["favorite"]:
                prompt["favorite"] = False
                print(f"\n❌ '{prompt['title']}' 프롬프트를 즐겨찾기에서 제거했습니다!")
            else:
                prompt["favorite"] = True
                print(f"\n⭐ '{prompt['title']}' 프롬프트를 즐겨찾기에 추가했습니다!")
        else:
            print("존재하지 않는 번호입니다.")
    except ValueError:
        print("숫자를 입력해주세요.")
```

**커밋**:
```powershell
git add prompt_manager.py
git commit -m "feat: 즐겨찾기 추가/해제 기능 구현"
```

---

### 3-12단계: 즐겨찾기 목록 보기 기능

**prompt_manager.py에 show_favorites() 함수 추가**:
```python
def show_favorites():
    """즐겨찾기된 프롬프트 목록을 보는 함수"""
    print("\n" + "="*40)
    print("즐겨찾기 목록")
    print("="*40)
    
    favorites = []
    for i, prompt in enumerate(prompts):
        if prompt["favorite"]:
            favorites.append((i + 1, prompt))
    
    if favorites:
        for num, prompt in favorites:
            print(f"{num}. [{prompt['category']}] {prompt['title']} ⭐")
        print(f"\n총 {len(favorites)}개의 즐겨찾기")
    else:
        print("즐겨찾기된 프롬프트가 없습니다.")
```

**브랜치 생성 (이 기능을 별도 브랜치에서 작업)**:
```powershell
git checkout -b feature/enhance-ui
```

**커밋**:
```powershell
git add prompt_manager.py
git commit -m "feat: 즐겨찾기 목록 기능 구현"
```

**main 브랜치로 복귀 및 병합**:
```powershell
git checkout main
git merge feature/enhance-ui
```

**결과**:
```
Updating 8bfa84d..9d7afef
Fast-forward
 prompt_manager.py | 21 insertions(+), 2 deletions(-)
```

---

### 3-13단계: 최종 테스트

**프로그램 실행**:
```powershell
python prompt_manager.py
```

**메뉴 2 선택** (프롬프트 목록):
```
========================================
프롬프트 목록
========================================
1. [텍스트 생성] 블로그 글 작성 도우미  
2. [이미지 생성] 제품 썸네일 생성  
3. [페르소나] IT 컨설턴트 페르소나  

총 3개의 프롬프트
```

**메뉴 5 선택** (상세 보기 - 1번):
```
========================================
프롬프트 상세 보기
========================================
번호 입력: 1

----------------------------------------
제목: 블로그 글 작성 도우미
카테고리: 텍스트 생성
즐겨찾기:  
----------------------------------------
내용:
당신은 10년 경력의 전문 블로거입니다. 주어진 주제에 대해 SEO에 최적화된 
블로그 글을 작성해주세요. 서론, 본론, 결론 구조를 갖추고, 독자의 관심을 
끄는 제목을 3개 제안해주세요.
----------------------------------------
```

**메뉴 0 선택** (종료):
```
프로그램을 종료합니다.
```

---

### 3-14단계: GitHub에 업로드

**원격 저장소 연결**:
```powershell
git remote add origin https://github.com/art-ruby/ia-codyssey.git
```

**커밋 이력 확인**:
```powershell
git log --oneline
```

**결과** (총 10개 커밋):
```
9d7afef (HEAD -> main, feature/enhance-ui) feat: 즐겨찾기 목록 기능 구현
8bfa84d feat: 즐겨찾기 추가/해제 기능 구현
6a91868 feat: 카테고리별 조회 기능 구현
8e6734a feat: 프롬프트 검색 기능 구현
97324e5 feat: 프롬프트 상세 보기 기능 구현
7bd7837 feat: 프롬프트 목록 보기 기능 구현
c07ddf9 feat: 프롬프트 추가 기능 구현
c3de5b9 feat: 프로그램 기본 구조 및 메뉴 구현
2198c45 feat: 기본 프롬프트 데이터 추가
8c43d96 chore: .gitignore 파일 생성
```

**GitHub 업로드**:
```powershell
git push -u origin main
```

**결과**:
```
Enumerating objects: 10, done.
Counting objects: 100% (10/10), done.
Delta compression using up to 8 threads
Compressing objects: 100% (8/8), done.
Writing objects: 100% (10/10), 1.23 KiB | 1.23 MiB/s, done.
Total 10 (delta 3), reused 0 (delta 0), pack-reused 0
To https://github.com/art-ruby/ia-codyssey.git
 * [new branch]      main -> main
 * [new branch]      feature/enhance-ui -> feature/enhance-ui
```

---

## 4. 프로그램 구조 및 코드

### 4-1. 파일 구조

```
prompt-manager/
├── .gitignore              (Python 캐시 및 환경파일 제외)
├── data.py                 (카테고리, 기본 프롬프트)
├── prompt_manager.py       (메인 프로그램, 8개 함수)
├── README.md               (프로젝트 설명서)
└── A1-1_결과보고서_최종.md (이 파일)
```

### 4-2. 핵심 파일 내용

**data.py (23줄)**
```python
# 카테고리 목록
CATEGORIES = ["텍스트 생성", "이미지 생성", "영상 생성", "페르소나", "자동화", "기타"]

# 기본 프롬프트 데이터
prompts = [
    {
        "title": "블로그 글 작성 도우미",
        "content": "당신은 10년 경력의 전문 블로거입니다. ...",
        "category": "텍스트 생성",
        "favorite": False
    },
    ...
]
```

**prompt_manager.py (238줄, 9개 함수)**

| 함수 | 기능 |
|---|---|
| `add_prompt()` | 새 프롬프트 추가 |
| `show_list()` | 전체 목록 표시 |
| `show_detail()` | 상세 내용 표시 |
| `search_prompt()` | 키워드 검색 |
| `show_by_category()` | 카테고리별 필터링 |
| `toggle_favorite()` | 즐겨찾기 토글 |
| `show_favorites()` | 즐겨찾기만 표시 |
| `show_menu()` | 메뉴 화면 |
| `main()` | 메인 루프 |

---

## 5. 기능 시연

### 프로그램 시작 화면

```powershell
$ python prompt_manager.py
```

```
========================================
    나만의 프롬프트 관리
========================================
1. 프롬프트 추가
2. 프롬프트 목록
3. 카테고리별 조회
4. 프롬프트 검색
5. 프롬프트 상세 보기
6. 즐겨찾기 관리
7. 즐겨찾기 목록
0. 종료
========================================
선택:
```

### 기능별 시연

**기능 1: 프롬프트 목록 (메뉴 2)**
```
========================================
프롬프트 목록
========================================
1. [텍스트 생성] 블로그 글 작성 도우미  
2. [이미지 생성] 제품 썸네일 생성  
3. [페르소나] IT 컨설턴트 페르소나  

총 3개의 프롬프트
```

**기능 2: 상세 보기 (메뉴 5 → 1번)**
```
========================================
프롬프트 상세 보기
========================================
번호 입력: 1

----------------------------------------
제목: 블로그 글 작성 도우미
카테고리: 텍스트 생성
즐겨찾기:  
----------------------------------------
내용:
당신은 10년 경력의 전문 블로거입니다. 주어진 주제에 대해 SEO에 최적화된 
블로그 글을 작성해주세요. 서론, 본론, 결론 구조를 갖추고, 독자의 관심을 
끄는 제목을 3개 제안해주세요.
----------------------------------------
```

**기능 3: 검색 (메뉴 4 → "블로그")**
```
========================================
프롬프트 검색
========================================
검색어: 블로그

검색 결과:
1. [텍스트 생성] 블로그 글 작성 도우미  

1개의 프롬프트를 찾았습니다.
```

**기능 4: 카테고리별 조회 (메뉴 3 → 1번)**
```
========================================
카테고리별 조회
========================================

카테고리 선택:
1) 텍스트 생성
2) 이미지 생성
3) 영상 생성
4) 페르소나
5) 자동화
6) 기타

선택: 1
[텍스트 생성] 카테고리 프롬프트:
1. 블로그 글 작성 도우미  

총 1개의 프롬프트
```

**기능 5: 즐겨찾기 추가 (메뉴 6 → 1번)**
```
========================================
즐겨찾기 관리
========================================
프롬프트 번호 입력: 1

⭐ '블로그 글 작성 도우미' 프롬프트를 즐겨찾기에 추가했습니다!
```

**기능 6: 즐겨찾기 목록 (메뉴 7)**
```
========================================
즐겨찾기 목록
========================================
1. [텍스트 생성] 블로그 글 작성 도우미 ⭐

총 1개의 즐겨찾기
```

**기능 7: 새 프롬프트 추가 (메뉴 1)**
```
========================================
프롬프트 추가
========================================
제목: 마케팅 카피 작성
내용: 당신은 전문 마케팅 카피라이터입니다. 제품의 특징을 강조하는 짧고 임팩트 있는 카피를 작성해주세요.

카테고리 선택:
1) 텍스트 생성
2) 이미지 생성
3) 영상 생성
4) 페르소나
5) 자동화
6) 기타
선택: 1

✅ '마케팅 카피 작성' 프롬프트가 추가되었습니다!
```

---

## 6. Git 커밋 기록

### 전체 커밋 이력

```powershell
git log --oneline --all
```

**결과** (총 10개 커밋):

```
9d7afef (feature/enhance-ui) feat: 즐겨찾기 목록 기능 구현
8bfa84d feat: 즐겨찾기 추가/해제 기능 구현
6a91868 feat: 카테고리별 조회 기능 구현
8e6734a feat: 프롬프트 검색 기능 구현
97324e5 feat: 프롬프트 상세 보기 기능 구현
7bd7837 feat: 프롬프트 목록 보기 기능 구현
c07ddf9 feat: 프롬프트 추가 기능 구현
c3de5b9 feat: 프로그램 기본 구조 및 메뉴 구현
2198c45 feat: 기본 프롬프트 데이터 추가
8c43d96 chore: .gitignore 파일 생성
```

### 사용된 Git 명령어

| 명령어 | 사용 목적 | 횟수 |
|---|---|---|
| `git init` | 저장소 초기화 | 1회 |
| `git add` | 파일 스테이징 | 11회 |
| `git commit` | 변경사항 커밋 | 10회 |
| `git push` | 원격 저장소 업로드 | 1회 |
| `git checkout -b` | 브랜치 생성 | 1회 |
| `git checkout` | 브랜치 전환 | 1회 |
| `git merge` | 브랜치 병합 | 1회 |
| `git log` | 커밋 이력 확인 | 1회 |

---

## 7. 요구사항 충족 확인

| # | 요구사항 | 충족 | 근거 |
|---|---|---|---|
| 1 | 메뉴 기반 콘솔 프로그램 | ✅ | show_menu() + main() 루프 |
| 2 | 7개 기능 이상 | ✅ | 7개 기능 모두 구현 |
| 3 | 기본 프롬프트 3개 | ✅ | data.py에 3개 등록 |
| 4 | 리스트·딕셔너리 사용 | ✅ | prompts 리스트 + dict 구조 |
| 5 | 함수 단위 분리 | ✅ | 9개 함수 구현 |
| 6 | 입력값 검증 | ✅ | while 루프 + try/except |
| 7 | 커밋 10개 이상 | ✅ | 정확히 10개 |
| 8 | 브랜치 작업 1회 이상 | ✅ | feature/enhance-ui 생성 및 병합 |
| 9 | Git 명령어 모두 사용 | ✅ | 8가지 명령어 모두 사용 |
| 10 | GitHub 업로드 | ✅ | https://github.com/art-ruby/ia-codyssey |

---

## 8. 학습 성과

### Python 기초 문법
- ✅ 변수, 자료형 (리스트, 딕셔너리, 문자열)
- ✅ 제어문 (if/elif/else, while, for)
- ✅ 함수 정의 및 호출
- ✅ 모듈 임포트 (from data import)
- ✅ 문자열 메서드 (.strip(), .lower())
- ✅ 예외 처리 (try/except)

### 데이터 구조 설계
- ✅ 리스트로 여러 항목 저장
- ✅ 딕셔너리로 항목의 속성 관리
- ✅ 리스트 필터링 및 검색
- ✅ enumerate로 인덱스와 값 동시 접근

### Git 버전 관리
- ✅ 저장소 초기화 (init)
- ✅ 기능별 의미 있는 커밋
- ✅ 브랜치 전략 (feature 브랜치)
- ✅ 브랜치 병합 (Fast-forward)
- ✅ 원격 저장소 연동

### 소프트웨어 개발 프로세스
- ✅ 요구사항 분석 및 기능 분해
- ✅ 단계적 개발 (Step-by-Step)
- ✅ 각 기능별 개별 테스트
- ✅ 최종 통합 테스트

---

**과제 완료: 2026년 8월 17일**
