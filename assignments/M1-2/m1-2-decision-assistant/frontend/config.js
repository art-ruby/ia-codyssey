/**
 * 배포 설정.
 *
 * Vercel 은 정적 호스팅이라 런타임에 환경 변수를 읽을 수 없다. 그래서
 * 백엔드 주소를 이 파일 한 곳에 두고, 배포 시 이 값만 바꾼다.
 * (또는 화면 우측 상단 ⚙ 에서 바꾸면 브라우저에 저장된다.)
 *
 * 우선순위: localStorage 에 저장된 값 > 아래 기본값
 */
window.API_BASE_DEFAULT = "http://127.0.0.1:8000";

/** Render 무료 플랜 콜드 스타트 대응 — 첫 요청 타임아웃을 넉넉히 잡는다. */
window.API_TIMEOUT_MS = 60000;
